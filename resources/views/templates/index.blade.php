<!-- Head --> 
@yield('head')

<!-- Nav -->
@yield('nav')

<!-- About -->
@yield('about')

<!-- Services -->
@yield('services')

<!-- Portfolio -->
@yield('portfolio')

<!-- Contact -->
@yield('contact')

